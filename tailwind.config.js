module.exports = {
    purge: ['./components/*.js', './pages/*.js', 'styles/*.css'],
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
};
